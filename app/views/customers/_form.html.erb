<%= form_for @customer do |f| %>
    <%= f.label :name, "Kund" %>
    <%= f.text_field :name %>
    <% if params[:action] == "new" %>
        <%= f.submit "Skapa kund" %>
    <% end %>
    <% if params[:action] == "show" %>
        <%= f.submit "Uppdatera kund" %>
    <% end %>
<% end %>